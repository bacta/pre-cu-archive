package $packageName;

import com.ocdsoft.bacta.soe.message.GameNetworkMessage;
import com.ocdsoft.bacta.soe.util.SOECRC32;
import lombok.Getter;

import java.nio.ByteBuffer;

/**
  $messageStruct
  */
@Getter
public final class $messageName extends GameNetworkMessage {

    static {
        priority = $priority;
        messageType = $messageSimpleName; // $opcode
    }

    public $messageName() {

    }

    public $messageName(final ByteBuffer buffer) {
        super(buffer);

    }

    @Override
    public void writeToBuffer(ByteBuffer buffer) {

    }
}
